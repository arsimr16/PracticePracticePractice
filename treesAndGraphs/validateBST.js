// implement a function to check whether a binary tree is a binary search tree

// I: a tree
// O: a boolean - whether or not the input is a BST
// C: none
// E: none (if the tree contains duplicates, it cannot be a BST);
const validateBST = (tree) => {
	
};
// time complexity:
// space complexity:

// tests
